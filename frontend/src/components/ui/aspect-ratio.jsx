import * as React from "react";
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

const AspectRatio = (props) => <AspectRatioPrimitive.Root {...props} />;

export { AspectRatio };
